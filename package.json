{
  "name": "mcp-ai-evidence-browser",
  "version": "0.3.0",
  "description": "AI Evidence Browser — MCP server that extracts verifiable evidence from the web for local LLMs",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsup src/index.ts --format esm --clean --no-dts --external better-sqlite3 --external @modelcontextprotocol/sdk --external zod --external jsdom --external @mozilla/readability --external cheerio",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "inspect": "npx @modelcontextprotocol/inspector node dist/index.js"
  },
  "keywords": [
    "mcp",
    "search",
    "privacy",
    "llm",
    "research"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@mozilla/readability": "^0.6.0",
    "better-sqlite3": "^12.9.0",
    "cheerio": "^1.2.0",
    "jsdom": "^29.0.2",
    "pdf-parse": "^2.4.5",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/jsdom": "^28.0.1",
    "tsup": "^8.5.1",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
